# Use CUDA 12.1 wheels (compatible with driver 535 / CUDA 12.2). Do NOT pip install torch from PyPI alone.
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.12.1
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.25.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
pyqubo>=1.2.0
dimod>=0.12.0
openjij>=0.7.0
pyyaml>=6.0
numpy>=1.24.0
tqdm>=4.65.0
evaluate>=0.4.0
bitsandbytes>=0.43.0
peft>=0.8.0
trl>=0.8.0
wandb>=0.16.0

# Optional: --unsloth flag on fast_finetune_pipeline.py / run_dpo.py. Free and
# open-source for single-GPU LoRA/QLoRA fine-tuning (which is what both scripts
# do); multi-GPU training requires paid Unsloth Pro, not needed here since a 3B
# model fits on one H100. Not used by tune_qubo_params.py -- that script never
# trains a model, so there is nothing for Unsloth to accelerate there.
unsloth>=2024.8
