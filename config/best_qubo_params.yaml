# ==============================================================================
# FILE: config/best_qubo_params.yaml
# ROLE: Optimal QUBO Hyperparameter Configuration
# BRANCH ADDITION (abhyuday): Newly created configuration file storing the
# best-performing QUBO objective coefficients discovered via the 81-combination
# grid search in `scripts/tune_qubo_params.py`. Used by the QUBO formulator during
# optimal reasoning chain selection.
# ==============================================================================

penalty_weight: 1.0
diversity_bonus: 0.1
cardinality_penalty: 0.05
answer_agree_weight: 0.3
answer_sim_weight: 0.7
validation_accuracy: 0.7778
notes: |
  Derived from partial grid search (52/81 combos evaluated, pw=1.5 not yet tested).
  All pw=1.0 combinations scored identically (7/9 correct on 10-question validation).
  First pw=1.0 combo used as canonical best. The untested pw=1.5 range may score
  equivalently or better, but pw=1.0 is a safe, well-supported choice.
