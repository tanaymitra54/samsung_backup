stale: false
penalty_weight: 1.0
diversity_bonus: 0.5
cardinality_penalty: 2.5
answer_agree_weight: 0.1
answer_sim_weight: 0.9
validation_accuracy: 0.903333
n_questions: 300
scoring: gold-free
combo_scoring: proxy
notes: Grid search over 300 questions, gold-free scoring, combo accuracy via proxy
  (majority vote over selected chains, no generation). Accuracy standard error about
  +/-2.9 points. Verify the selected cardinality_penalty yields a multi-chain subset;
  run_all_benchmarks.py warns if the mean drops below 2.
